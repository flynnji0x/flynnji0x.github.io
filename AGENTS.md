# 项目约定

## Git 提交规则

- **不要自动执行 git commit**。修改代码后停在"已完成、待提交"状态即可。
- 仅当用户明确要求提交(如说"commit"、"提交一下")时才执行 git commit。
- 提交时遵循 Conventional Commits 规范(type: description,如 `feat:`、`fix:`、`chore:`),必要时附 body 说明。
